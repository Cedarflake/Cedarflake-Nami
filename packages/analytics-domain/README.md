# Analytics domain

Shared analytics event validation, canonical event types, query result types, and Store behavior constants used by collector hosts and storage plugins. The package owns cross-backend semantics such as retention, entry-domain lookback, and result limits, but contains no database driver, framework cache, or provider SDK.

---

English · [简体中文](README.zh-CN.md)
